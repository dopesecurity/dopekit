[tool.poetry]
name = "example_lambda"
version = "0.1.0"
description=""
authors=[]

[tool.poetry.dependencies]
python = "^3.11"
structlog = "^24.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
